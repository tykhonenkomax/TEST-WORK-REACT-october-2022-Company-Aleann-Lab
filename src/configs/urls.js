const baseURL = 'https://api.json-generator.com'
const mapUrls = 'http://api.openweathermap.org/geo/1.0/reverse?'

const urls = {
    jobs: '/templates/ZM1r0eic3XEy/data'
}

export {
    baseURL,
    urls,
    mapUrls
}